{
	"name": "sample-app",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"workspaces": [
		"src",
		"api",
		"web-worker"
	],
	"dependencies": {
		"cheerio": "^1.1.2",
		"dompurify": "^3.2.3",
		"marked": "^15.0.6",
		"wirejs-components": "^0.1.91-llm",
		"wirejs-dom": "^1.0.44",
		"wirejs-module-payments-stripe": "^0.1.42-llm",
		"wirejs-resources": "^0.1.148-llm",
		"wirejs-web-worker": "^1.0.45-llm"
	},
	"devDependencies": {
		"typescript": "^5.7.3",
		"wirejs-scripts": "^3.0.146-llm"
	},
	"scripts": {
		"prebuild": "npm run prebuild --workspaces --if-present",
		"prestart": "npm run prestart --workspaces --if-present",
		"start": "wirejs-scripts ws-run-parallel start",
		"start:public": "wirejs-scripts ws-run-parallel start:public",
		"build": "npm run build --workspaces --if-present"
	},
	"engines": {
		"node": ">=20.0.0"
	}
}
