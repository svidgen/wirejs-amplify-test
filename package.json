{
	"name": "sample-app",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"workspaces": [
		"src",
		"api"
	],
	"dependencies": {
		"dompurify": "^3.2.3",
		"marked": "^15.0.6",
		"wirejs-dom": "^1.0.41",
		"wirejs-resources": "^0.1.76-realtime",
		"wirejs-components": "^0.1.19-realtime"
	},
	"devDependencies": {
		"wirejs-scripts": "^3.0.74-realtime",
		"typescript": "^5.7.3"
	},
	"scripts": {
		"prebuild": "npm run prebuild --workspaces --if-present",
		"prestart": "npm run prestart --workspaces --if-present",
		"start": "wirejs-scripts start",
		"build": "wirejs-scripts build"
	},
	"engines": {
		"node": ">=20.0.0"
	}
}