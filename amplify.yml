version: 1
backend:
  phases:
    preBuild:
      commands:
        - nvm install 22
    build:
      commands:
        - npx wirejs-deploy-amplify-basic@realtime prebuild
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
        - npx ampx generate outputs --branch $AWS_BRANCH --app-id $AWS_APP_ID
frontend:
  phases:
    build:
      commands:
        - npx wirejs-deploy-amplify-basic@realtime inject-backend
        - npm run build
        - npx wirejs-deploy-amplify-basic@realtime build-hosting-artifacts
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
